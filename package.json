{
  "devDependencies": {
    "happy-dom": "^9.9.2",
    "vitest": "^0.30.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  }
}
